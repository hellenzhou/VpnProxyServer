# 🚀 极简版CMakeLists.txt
cmake_minimum_required(VERSION 3.10)
project(vpn_server_simple)

# 设置C++标准
set(CMAKE_CXX_STANDARD 17)

# 查找必要的库
find_package(hilog REQUIRED)

# 源文件
set(SOURCES
    vpn_server_simple.cpp
    packet_forwarder_simple.cpp
)

# 创建共享库
add_library(vpn_server_simple SHARED ${SOURCES})

# 链接库
target_link_libraries(vpn_server_simple
    hilog_ndk.z
    ${CMAKE_DL_LIBS}
)

# 编译选项
target_compile_options(vpn_server_simple PRIVATE
    -Wall
    -Wextra
    -O2
)
